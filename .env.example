# Copy to .env and fill in real values. .env is gitignored - never commit it.
# Directory where gpu-debug.sh writes its logs:
LOG_DIR="$HOME/logs"
# Local hostname (kept out of anything you share publicly):
GPU_HOST="your-hostname"
# GPU PCI address as shown by lspci:
GPU_PCI="01:00.0"
